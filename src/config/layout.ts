// Layout constants
export const HEADER_HEIGHT = 80; // Height of the fixed header in pixels
export const SCROLL_OFFSET = 32; // Additional offset for better spacing

export const LAYOUT = {
  headerHeight: HEADER_HEIGHT,
  scrollOffset: SCROLL_OFFSET,
  totalOffset: HEADER_HEIGHT + SCROLL_OFFSET
};